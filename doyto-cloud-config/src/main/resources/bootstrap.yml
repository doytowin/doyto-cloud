server:
  port: @docker.expose@

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          username: f0rb@icloud.com
          password: os--0404
          uri: https://git.oschina.net/doyto/doyto-config.git

info:
  version: @project.version@

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:eureka}:8761/eureka/

#encrypt:
#  failOnError: false
#  key-store:
#    location: classpath:keystore.jks
#    password: ${KEYSTORE_PASSWORD:doyto-cloud} # don't use a default in production
#    alias: test
