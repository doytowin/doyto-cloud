server:
  port: 8080

sidecar:
  port: 8000

spring:
  application:
    name: doyto-cloud-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: doyto-cloud-config

eureka:
  instance:
    prefer-ip-address: true
#    non-secure-port: 0
  client:
    fetch-registry: true
#    register-with-eureka: true

#encrypt:
#  failOnError: false
#  key-store:
#    location: classpath:keystore.jks
#    password: ${KEYSTORE_PASSWORD:doyto-cloud} # don't use a default in production
#    alias: test
