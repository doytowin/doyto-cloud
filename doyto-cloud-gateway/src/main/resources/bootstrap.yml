server:
  port: 8080

#sidecar:
#  port: 8000

spring:
  application:
    name: doyto-cloud-gateway
  cloud:
    config:
      discovery:
        enabled: true

info:
  version: @project.version@

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:eureka}:8761/eureka/

#encrypt:
#  failOnError: false
#  key-store:
#    location: classpath:keystore.jks
#    password: ${KEYSTORE_PASSWORD:doyto-cloud} # don't use a default in production
#    alias: test
